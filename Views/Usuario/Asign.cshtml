@using AxonAccessMVC.Models.Clases;

@model AxonAccessMVC.Models.Mae_Sucursal
@{
    ViewBag.Title = "CreateSeleccion";
}

<!DOCTYPE html>
<html>
<head>
    <link href="~/styles/css/style.css" rel='stylesheet' type='text/css' media="all">
    <link href="//fonts.googleapis.com/css?family=Saira+Condensed:400,500,600,700" rel="stylesheet">
    <script type="text/javascript" src="js/jquery.js"></script>
    <script>
        function ddlselect() {
            var d = document.getElementById("ddselect").value;
            console.log(d);
        }
        ddlselect();
    </script>
</head>
<body>

    <form action="~/Usuario/Asign2" method="post">
        <input type="hidden" id="ArticleId" name="ArticleId" value="@ViewBag.id" />
        <div class="online-w3l-form">
            <div class="form-left-w3l">
                <select name="id_valor" id="ddselect" onchange="ddlselect();" class="form-control buttom" @Html.DropDownListFor(p => p.id_sucursal, new SelectList(ViewBag.sucursals, "Id_Sucursal", "descripcion")) </select>
            </div>
            <div class="btnn">
                <input type="submit" value="Asignar Sucursal">
            </div>
        </div>

    </form>

</body>
</html>
